# Environment Variables for Vercel Deployment
# Copy these to your Vercel project settings > Environment Variables section

# Server settings
NODE_ENV=production

# MongoDB Connection (replace with your actual MongoDB Atlas connection string)
MONGO_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/lms?retryWrites=true&w=majority

# Authentication
JWT_SECRET=your_secure_jwt_secret_key_here
JWT_EXPIRE=7d

# API Configuration
API_URL=https://your-vercel-app.vercel.app